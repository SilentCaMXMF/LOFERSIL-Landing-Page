/**
 * Telegram Plugin Simulation Script - DISABLED
 *
 * This script has been disabled to prevent automatic execution during startup.
 * The "Success! Telegram notification sent!" message was appearing at OpenCode startup.
 *
 * To re-enable simulation functionality, remove this comment block and restore the original code.
 *
 * Original purpose: Test complete notification flow for Telegram plugin
 * - Simulates message capture from user input
 * - Simulates session idle detection
 * - Sends idle notification to Telegram
 */

console.log('🚫 Telegram plugin simulation is disabled to prevent startup messages');

// Disabled simulation function
/*
import SimpleTelegramBot from './lib/SimpleTelegramBot.js';
import { config } from 'dotenv';
import { SimulationError, ErrorHandler } from './lib/errors.js';
import { SecurityAuditLogger } from './lib/security-utils.js';
import { messageConfig, timeoutConfig } from './lib/config.js';

// Load environment variables from project root
config({ path: '../../.env' });

async function testPluginNotificationFlow() {
  // ... original simulation code here
}

// Run the test
testPluginNotificationFlow().catch(console.error);
*/
