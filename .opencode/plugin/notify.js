export const Notify = async ({ $ }) => {
    return {
    // async event(input) {
    //   if (input.event.type === "session.idle") {
    //     await $`say "Your code is done!"`
    //   }
    // },
    };
};
