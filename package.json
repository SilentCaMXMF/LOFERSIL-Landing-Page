{
  "name": "lofersil-landing-page",
  "version": "1.0.0",
  "description": "LOFERSIL Landing Page - Static TypeScript site for Vercel deployment",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run build:compile && npm run build:css && npm run build:copy && npm run build:seo",
    "build:compile": "tsc",
    "build:css": "postcss src/styles/main.css -o dist/main.css",
    "build:copy": "mkdir -p dist/scripts && rm -rf dist/scripts/modules && mv dist/modules dist/scripts/ && mv dist/index.js dist/scripts/ && rm -f dist/*.map dist/scripts/modules/*.map && cp index.html dist/ && cp -r assets dist/ && cp -r src/locales dist/ && cp privacy.html dist/ && cp terms.html dist/ && cp assets/offline.html dist/ && cp node_modules/dompurify/dist/purify.min.js dist/dompurify.min.js",
    "build:seo": "echo '<?xml version=\"1.0\" encoding=\"UTF-8\"?><urlset xmlns=\"http://www.sitemaps.org/schemas/sitemap/0.9\"><url><loc>https://lofersil-landing-page.vercel.app/</loc><lastmod>'$(date -Iseconds)'</lastmod><changefreq>monthly</changefreq><priority>1.0</priority></url></urlset>' > dist/sitemap.xml && echo 'User-agent: *\nAllow: /\n\nSitemap: https://lofersil-landing-page.vercel.app/sitemap.xml' > dist/robots.txt",
    "build:dev": "tsc && postcss src/styles/main.css -o dist/main.css",
    "dev": "tsc --watch",
    "start": "serve dist --single -l 3000",
    "lint": "eslint src/**/*.ts",
    "format": "prettier --write src/**/*.{ts,css}",
    "prebuild": "npm run format"
  },
  "keywords": [
    "landing-page",
    "typescript",
    "static-site",
    "vercel",
    "lofersil"
  ],
  "author": "LOFERSIL",
  "license": "MIT",
  "devDependencies": {
    "@types/dompurify": "^3.2.0",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "autoprefixer": "^10.4.0",
    "cssnano": "^6.0.0",
    "eslint": "^9.0.0",
    "postcss": "^8.4.0",
    "postcss-cli": "^10.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": "22.x"
  },
  "packageManager": "npm@10.0.0",
  "dependencies": {
    "dompurify": "^3.3.0"
  }
}
